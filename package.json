{
  "name": "fileorganizer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/shared",
    "packages/engine",
    "packages/ui"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run build --workspace=@fileorganizer/shared && npm run test --workspaces --if-present",
    "lint": "eslint \"packages/*/src/**/*.{ts,tsx}\"",
    "typecheck": "npm run build --workspace=@fileorganizer/shared && npm run typecheck --workspaces --if-present",
    "start": "npm run build --workspace=@fileorganizer/shared && npm run build --workspace=@fileorganizer/ui && npm run start --workspace=@fileorganizer/engine",
    "fetch-binaries": "tsx scripts/fetch-binaries.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "eslint": "^9.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0"
  }
}
